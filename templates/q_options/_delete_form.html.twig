<form method="post" action="{{ path('q_options_delete', {'id': q_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ q_option.id) }}">
    <button class="btn">Delete</button>
</form>
